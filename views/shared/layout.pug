html
    head
        meta(charset='utf-8')

        if (title !== undefined)
            title #{title} - Corona
        else
            title Corona

        //Styles
        include styles

    body
        include navbar

        div(class='container')
            hr(class='text-hide')
            block content

        //scripts
        include scripts

        block temp-scripts

